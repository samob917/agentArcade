{
  "name": "@agent-arcade/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "migrate:neon": "tsx src/migrate.ts",
    "studio": "drizzle-kit studio",
    "clean": "rm -rf dist",
    "lint": "echo 'no linter configured'"
  },
  "dependencies": {
    "@agent-arcade/shared": "*",
    "@neondatabase/serverless": "^0.10",
    "drizzle-orm": "^0.39",
    "postgres": "^3.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30",
    "typescript": "^5.7"
  }
}
